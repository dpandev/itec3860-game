package avengers.domain.utils;

/** A placeholder class for CommandToken in the Avengers game domain. */
public record CommandToken(String s, String t) {
  /** Creates a new CommandToken. */
  public CommandToken {}
}
